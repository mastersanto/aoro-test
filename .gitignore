# Dependencies
node_modules/

# Builds
.next/
out/
dist/
build/

# Environment — only .env.example is committed
.env
.env.*
!.env.example

# Hosting
.vercel/
.netlify/

# Test output
coverage/
test-results/
playwright-report/
blob-report/

# Tooling caches
*.tsbuildinfo
next-env.d.ts
.eslintcache

# Seeded design-canvas output (2.5MB generated page; rebuilt from the .dc.html
# sources beside it and already hosted as the shared artifact)
design/**/polymarket-widget-terminal.html

# Editor/OS noise
.DS_Store
*.log

# Seeded design canvases: ~2.5MB each and almost entirely editor code. The design
# CONTENT is the .dc.html artboards and canvas.json beside them, which are tracked;
# the canvas is regenerated from those and published as an artifact.
design/**/polymarket-*.html
