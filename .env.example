# Copy to .env.local and fill in. Server-side only — none of these may reach the client bundle (constitution Article IV).

# AI market-selection assistance (Claude API) — pending spec Q5
ANTHROPIC_API_KEY=

# Polymarket's Gamma API (market data) is public — no key needed.
# Bets are signed by the user's wallet client-side (Article III); add server-side
# Polymarket credentials here only if an approved plan.md explicitly calls for them.
